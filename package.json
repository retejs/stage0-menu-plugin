{
  "name": "stage0-rete-context-menu-plugin",
  "version": "1.0.0-beta2",
  "description": "Rete context menu using https://www.npmjs.com/package/stage0 (~1.6 kb framewok) renderer",
  "main": "build/context-menu-plugin.min.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/trsh/stage0-rete-context-menu-plugin"
  },
  "scripts": {
    "build": "rete --build rete.config.js",
    "build:dev": "rete --build rete.config.js --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "stage0": "^0.0.11"
  },
  "devDependencies": {
    "rete-cli": "^0.2.0",
    "rollup-plugin-sass": "^0.6.1"
  }
}
